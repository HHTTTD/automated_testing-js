# 🚀 Quick Start - เริ่มต้นใช้งานด่วน

## สำหรับมือใหม่ที่ไม่เคยใช้ Playwright มาก่อน

### ขั้นตอนที่ 1: ตรวจสอบว่าติดตั้งแล้วหรือยัง

เปิด Command Prompt หรือ Terminal แล้วพิมพ์:

```bash
node --version
npm --version
```

ถ้าขึ้น error แสดงว่ายังไม่ได้ติดตั้ง Node.js → ไปดาวน์โหลดที่ https://nodejs.org

### ขั้นตอนที่ 2: เปิดโฟลเดอร์โปรเจค

```bash
cd c:\Users\Lenovo\Desktop\Test\Automated_testing\test-01
```

### ขั้นตอนที่ 3: ติดตั้ง (ทำครั้งแรกครั้งเดียว)

```bash
npm install
npx playwright install
```

รอจนกว่าจะเสร็จ (อาจใช้เวลา 5-10 นาที)

### ขั้นตอนที่ 4: รัน Test ครั้งแรก

**วิธีที่ 1: สำหรับ Windows (ง่ายที่สุด!)**
- ดับเบิลคลิกที่ไฟล์ `run-tests.bat`
- เลือก `3` (Run login tests only)
- กด Enter

**วิธีที่ 2: ใช้คำสั่ง**
```bash
npx playwright test login.spec.js --workers=1
```

### ขั้นตอนที่ 5: ดู Test Report

```bash
npx playwright show-report
```

## คำสั่งที่ใช้บ่อย

### 🎯 รัน Test แบบต่างๆ

```bash
# รัน test ทั้งหมด
npm test

# รัน test แบบเห็น browser (แนะนำสำหรับมือใหม่!)
npx playwright test --headed --workers=1

# รัน test แบบ UI Mode (ดีที่สุด!)
npx playwright test --ui

# รัน test เฉพาะไฟล์เดียว
npx playwright test login.spec.js --workers=1

# รัน test ที่มีคำว่า "login"
npx playwright test -g "login" --workers=1
```

### 📊 ดูผลลัพธ์

```bash
# ดู test report
npx playwright show-report

# ดู test report ในหน้าเว็บ
npm run test:report
```

### 🐛 Debug Tests

```bash
# รันแบบ debug (แนะนำเมื่อ test ล้มเหลว)
npx playwright test login.spec.js --debug --workers=1
```

## โครงสร้างโปรเจคแบบง่าย

```
test-01/
├── tests/                    👈 โฟลเดอร์เก็บไฟล์ test
│   ├── login.spec.js        👈 ทดสอบการ Login
│   ├── products.spec.js     👈 ทดสอบหน้าสินค้า
│   ├── cart.spec.js         👈 ทดสอบตะกร้า
│   └── ...
├── playwright.config.js      👈 ไฟล์ตั้งค่า
├── package.json              👈 ไฟล์จัดการ dependencies
└── run-tests.bat            👈 ไฟล์สำหรับรัน test (Windows)
```

## ไฟล์ Test ที่มีในโปรเจค

| ไฟล์ | จำนวน Tests | คำอธิบาย |
|------|-------------|----------|
| `login.spec.js` | 7 tests | ทดสอบการ Login |
| `products.spec.js` | 9 tests | ทดสอบหน้าสินค้า |
| `cart.spec.js` | 11 tests | ทดสอบตะกร้าสินค้า |
| `checkout.spec.js` | 13 tests | ทดสอบการชำระเงิน |
| `e2e-complete-flow.spec.js` | 5 tests | ทดสอบ End-to-End |
| `example-quick-test.spec.js` | 7 tests | ตัวอย่างสำหรับเรียนรู้ |
| `example-with-helpers.spec.js` | 6 tests | ตัวอย่างใช้ helper functions |

**รวมทั้งหมด: 58 test cases × 3 browsers = 174 tests!**

## Tips สำหรับมือใหม่

### 1. เริ่มต้นด้วย UI Mode

```bash
npx playwright test --ui
```

UI Mode จะช่วยให้คุณ:
- เห็น test ทำงานแบบ step-by-step
- เข้าใจว่า test แต่ละตัวทำอะไร
- Debug ได้ง่าย

### 2. รัน Test ทีละไฟล์

อย่ารัน test ทั้งหมดในครั้งแรก! เริ่มจากไฟล์เดียว:

```bash
npx playwright test example-quick-test.spec.js --workers=1
```

### 3. ใช้ Headed Mode

เพื่อเห็น browser ทำงาน:

```bash
npx playwright test login.spec.js --headed --workers=1
```

### 4. อ่านไฟล์ตัวอย่าง

เปิดไฟล์เหล่านี้ใน VS Code หรือ Notepad:
- `tests/example-quick-test.spec.js` - ตัวอย่างพื้นฐาน
- `tests/example-with-helpers.spec.js` - ตัวอย่างขั้นสูง
- `tests/login.spec.js` - test จริง

### 5. ทดลองแก้ไขโค้ด

ลองเปลี่ยนค่าต่างๆ ในไฟล์ test แล้วรันดู:
- เปลี่ยน username
- เปลี่ยน timeout
- เพิ่ม console.log

## คำสั่งแบบละเอียด (สำหรับคนที่อยากเข้าใจ)

### รัน Test บน Browser เดียว

```bash
# Chrome
npx playwright test --project=chromium --workers=1

# Firefox
npx playwright test --project=firefox --workers=1

# Safari (WebKit)
npx playwright test --project=webkit --workers=1
```

### รัน Test เฉพาะบางไฟล์

```bash
# รัน login tests เท่านั้น
npx playwright test login.spec.js --workers=1

# รัน cart และ checkout tests
npx playwright test cart.spec.js checkout.spec.js --workers=1
```

### รัน Test เฉพาะชื่อ

```bash
# รัน test ที่มีคำว่า "successfully"
npx playwright test -g "successfully" --workers=1

# รัน test ที่มีคำว่า "error"
npx playwright test -g "error" --workers=1
```

## การแก้ปัญหาเบื้องต้น

### ปัญหา: ไม่สามารถรันคำสั่ง npm ได้

**แก้ไข:** ติดตั้ง Node.js จาก https://nodejs.org

### ปัญหา: Test timeout

**แก้ไข:** เพิ่ม `--timeout=60000` (60 วินาที)
```bash
npx playwright test login.spec.js --timeout=60000 --workers=1
```

### ปัญหา: Test ล้มเหลว

**แก้ไข:** รันแบบ debug เพื่อดูว่าเกิดอะไรขึ้น
```bash
npx playwright test login.spec.js --debug --workers=1
```

### ปัญหา: หน้าเว็บโหลดช้า

**แก้ไข:** รันด้วย slower user (performance_glitch_user) หรือเพิ่ม timeout

### ปัญหา: Error "spawn EPERM"

**แก้ไข:** ปิด antivirus ชั่วคราว หรือรัน Command Prompt แบบ Administrator

## ลำดับการเรียนรู้แนะนำ

### สัปดาห์ที่ 1: พื้นฐาน
1. รัน test ที่มีอยู่แล้วทั้งหมด
2. ดู test report
3. ทดลองใช้ UI Mode
4. อ่านโค้ดในไฟล์ `example-quick-test.spec.js`

### สัปดาห์ที่ 2: ทำความเข้าใจ
1. อ่านโค้ดใน `login.spec.js` ทีละ test
2. ทดลองแก้ไขโค้ดเล็กน้อย
3. ลองเพิ่ม `console.log` เพื่อดูค่าต่างๆ
4. ทดลองทำให้ test ล้มเหลวดู

### สัปดาห์ที่ 3: เขียนเอง
1. ลองเขียน test ง่ายๆ ใน `example-quick-test.spec.js`
2. ทดลองใช้ helper functions
3. สร้างไฟล์ test ใหม่ของตัวเอง
4. ลองเขียน test สำหรับเว็บไซต์อื่น

## ทรัพยากรเพิ่มเติม

### เอกสารภาษาไทย
- 📄 `GUIDE-TH.md` - คู่มือฉบับเต็มภาษาไทย
- 📄 `README.md` - คู่มือภาษาอังกฤษ

### ไฟล์ตัวอย่าง
- 📝 `tests/example-quick-test.spec.js`
- 📝 `tests/example-with-helpers.spec.js`

### เว็บไซต์
- 🌐 [Playwright Official Docs](https://playwright.dev)
- 🌐 [Playwright API](https://playwright.dev/docs/api/class-playwright)

## คำแนะนำสุดท้าย

1. **อย่ากลัวที่จะลองผิดลองถูก** - Test ล้มเหลวเป็นเรื่องปกติ
2. **ใช้ UI Mode บ่อยๆ** - มันช่วยให้เข้าใจง่ายขึ้นมาก
3. **อ่านโค้ดที่มีอยู่** - เรียนรู้จากตัวอย่างที่มีอยู่แล้ว
4. **ทดลองแก้ไข** - แก้โค้ดเล็กๆ น้อยๆ แล้วรันดู
5. **ใช้ console.log** - พิมพ์ค่าออกมาดูเพื่อเข้าใจ

---

**เริ่มต้นเลยตอนนี้!**

```bash
# เปิด UI Mode และเริ่มเล่น!
npx playwright test --ui
```

**สนุกกับการเรียนรู้! 🎉**


